spring:
  output:
    ansi:
      enabled: always
  messages:
    basename: language/messages
    encoding: UTF-8
  shell:
    interactive:
      enabled: false
  main:
    allow-circular-references: true

application:
  locale: other
